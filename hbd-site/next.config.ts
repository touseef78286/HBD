import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Vercel automatically handles Next.js optimization
  // No need for static export or basePath on Vercel
};

export default nextConfig;
